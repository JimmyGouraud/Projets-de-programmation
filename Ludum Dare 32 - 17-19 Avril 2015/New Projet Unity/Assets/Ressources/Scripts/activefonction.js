#pragma strict

class activerFonction extends MonoBehaviour{
	/*private var go : GameObject;
	private var light : Light;
	private var inter : Interrupteur;


	function activerFonction(){
		if(go.tag == "Interrupteur"){
			if(inter.interrupteurOn == false){
				GetComponent(Light).enabled = false;
			}else{
				GetComponent(Light).enabled = true;
			}
		}
		//collider(Light).enabled = true;
	}
	
	function Update(){
		activerFonction();
	}*/
}